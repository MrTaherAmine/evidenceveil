# Contributing

Use synthetic fixtures only. Do not commit customer logs, production secrets, real incident evidence, `.evlt` vaults, private keys, or credentials. Run `pytest`, `ruff check .`, `ruff format --check .`, and `mypy src/` before opening a pull request.

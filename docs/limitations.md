# Limitations

EvidenceVeil cannot guarantee detection of every identifier or secret, determine legal anonymisation, eliminate linkage attacks, securely erase Python memory, rewrite binary EVTX, rewrite PCAP, sandbox plugins, or replace organizational disclosure governance. Monolithic JSON may require memory proportional to the document size; JSONL/CSV/text paths are the preferred streaming formats for very large data.

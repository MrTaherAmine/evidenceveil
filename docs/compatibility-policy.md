# Compatibility Policy

The v1 public CLI and policy schema use semantic versioning. Python 3.11+ is supported. Breaking CLI, policy-schema or vault-envelope changes require a major version unless a security issue requires a documented exception.

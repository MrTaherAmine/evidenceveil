# Vault Recovery

`evidenceveil restore` authenticates the `.evlt` envelope before producing restored files. A wrong passphrase or tampered vault fails closed. Redaction, dropping, generalization, and other irreversible transformations cannot be recovered from the mapping vault.

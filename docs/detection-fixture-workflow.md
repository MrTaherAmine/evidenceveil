# Detection Fixture Workflow

Use the `detection-fixture` policy with a utility contract that identifies fields needed by the target analytic. Use the utility contract as a lightweight structural checkpoint, then independently confirm required fields, relationships, and temporal order remain suitable for the target analytic; v1.0 does not prove those semantic properties automatically.

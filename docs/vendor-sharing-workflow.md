# Vendor Sharing Workflow

Use `vendor-support` as a starting point, remove authentication material, pseudonymize identities and internal infrastructure, preserve only troubleshooting-relevant relationships, and keep the vault outside the transfer package.

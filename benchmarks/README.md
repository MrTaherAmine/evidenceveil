# Benchmarks

Benchmark claims are intentionally not hard-coded before measurement. For release benchmarking, generate a synthetic JSONL/CSV corpus, measure records/sec, bytes/sec and peak RSS on named hardware, and record detector/vault overhead. Do not use real incident evidence.
